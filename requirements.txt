# NYC TLC Reliability Pipeline — Python dependencies
# Pin minor versions to keep behaviour reproducible across environments.
# Install: pip install -r requirements.txt

# ── Core data ────────────────────────────────────────────────────────────────
pandas>=2.0.0,<3.0.0
pyarrow>=14.0.0,<18.0.0      # Parquet I/O; pq.read_metadata() used in ingest.py

# ── Analytical SQL ────────────────────────────────────────────────────────────
duckdb>=0.10.0,<2.0.0        # In-process SQL for fact+dimension joins and metric queries

# ── Socrata API (Mode 2 retrieval) ────────────────────────────────────────────
sodapy>=2.2.0,<3.0.0         # Official Socrata Python client
requests>=2.31.0,<3.0.0      # HTTP downloads (Parquet bulk, CSV fallback)

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=7.4.0,<9.0.0

# ── Notebook (optional — profiling scratch work only) ─────────────────────────
# jupyter>=1.0.0   # Uncomment if using notebooks/exploration.ipynb

# stdlib (no install needed): hashlib, logging, argparse, json, pathlib, datetime
